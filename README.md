# Dalvonic
Semantic analysis + Tweets + Pretty Charts

# Quick setup:
Install Flask:
`pip install flask`

# Usage

`python app.py`

# Dependencies

pip install: nltk, tweepy, whatever module is missing. Trust me.
